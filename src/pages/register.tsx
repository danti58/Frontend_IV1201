// pages/login.tsx
import React from 'react';
import RegisterPresenter from '../app/components/register/registerPresenter';

/**
 * Page component for the register page.
 * 
 * @returns - Register presenter component
 */
export default function Register() {
  return (
    <div>
      <RegisterPresenter />
    </div>
  );
}

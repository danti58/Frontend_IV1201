// Import necessary dependencies
/*import { useDispatch } from 'react-redux';
import { setAuthData } from './actions';

// Define a functional component to handle the API response
const ApiResponseHandler = (responseData) => {
  // Get the dispatch function using the useDispatch hook
  const dispatch = useDispatch();

  // Handle the API response inside the functional component
  const handleApiResponse = (responseData) => {
    const { token, currentUser } = responseData;

    // Dispatch action to update Redux store with token and currentUser
    dispatch(setAuthData(token, currentUser));
  };

  // Call the handleApiResponse function
  handleApiResponse(responseData);

  // Return null (or you can return something else if needed)
  return null;
};

// Export the functional component
export default ApiResponseHandler;
*/
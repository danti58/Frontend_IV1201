// pages/login.tsx
import React from 'react';
import PasswordResetLinkRequestPresenter from '@/app/components/passwordReset/passwordResetLinkRequestPresenter';

/**
 * Page component for the password reset link request page.
 * 
 * @returns - Password reset link request presenter component
 */
export default function PasswordResetLinkRequestPage() {
  return <PasswordResetLinkRequestPresenter />;
}

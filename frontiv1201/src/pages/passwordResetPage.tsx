import react from 'react';
import PasswordResetPresenter from '../app/components/passwordReset/passwordResetPresenter';

/**
 * Page component for the password reset page.
 * 
 * @returns - Password reset presenter component
 */
export default function PasswordResetPage() {
  return (
    <div>
      <PasswordResetPresenter />
    </div>
  );
}